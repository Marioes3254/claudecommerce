<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ClawCommerce — Your Shopify AI Employee</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0a;
    --surface: #111111;
    --border: #1e1e1e;
    --accent: #00e87a;
    --accent2: #ff6b35;
    --text: #f0f0f0;
    --muted: #666;
    --card: #131313;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'DM Mono', monospace;
    overflow-x: hidden;
  }

  /* Noise texture overlay */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 9999;
    opacity: 0.4;
  }

  /* NAV */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 48px;
    border-bottom: 1px solid var(--border);
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(12px);
  }

  .nav-logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 20px;
    letter-spacing: -0.5px;
  }

  .nav-logo span { color: var(--accent); }

  .nav-cta {
    background: var(--accent);
    color: #000;
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    font-weight: 500;
    padding: 10px 20px;
    border: none;
    cursor: pointer;
    letter-spacing: 0.5px;
    transition: all 0.2s;
    text-decoration: none;
  }

  .nav-cta:hover { background: #00ff88; transform: translateY(-1px); }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 120px 48px 80px;
    position: relative;
    overflow: hidden;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(0,232,122,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,232,122,0.04) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 50%, black 0%, transparent 100%);
  }

  .hero-tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--accent);
    border: 1px solid rgba(0,232,122,0.3);
    padding: 6px 14px;
    width: fit-content;
    margin-bottom: 32px;
    animation: fadeUp 0.6s ease both;
  }

  .hero-tag::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--accent);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  h1 {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: clamp(48px, 8vw, 96px);
    line-height: 0.95;
    letter-spacing: -3px;
    max-width: 900px;
    animation: fadeUp 0.6s 0.1s ease both;
  }

  h1 em {
    font-style: normal;
    color: var(--accent);
    position: relative;
  }

  h1 em::after {
    content: '';
    position: absolute;
    bottom: 4px; left: 0; right: 0;
    height: 3px;
    background: var(--accent);
    opacity: 0.4;
  }

  .hero-sub {
    font-size: 16px;
    color: var(--muted);
    max-width: 480px;
    line-height: 1.7;
    margin-top: 28px;
    font-weight: 300;
    animation: fadeUp 0.6s 0.2s ease both;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 20px;
    margin-top: 40px;
    animation: fadeUp 0.6s 0.3s ease both;
  }

  .btn-primary {
    background: var(--accent);
    color: #000;
    font-family: 'DM Mono', monospace;
    font-size: 14px;
    font-weight: 500;
    padding: 16px 32px;
    border: none;
    cursor: pointer;
    letter-spacing: 0.5px;
    transition: all 0.2s;
    text-decoration: none;
    display: inline-block;
  }

  .btn-primary:hover { background: #00ff88; transform: translateY(-2px); box-shadow: 0 8px 30px rgba(0,232,122,0.3); }

  .btn-ghost {
    color: var(--muted);
    font-size: 13px;
    text-decoration: none;
    border-bottom: 1px solid var(--border);
    padding-bottom: 2px;
    transition: color 0.2s, border-color 0.2s;
  }

  .btn-ghost:hover { color: var(--text); border-color: var(--muted); }

  /* STATS BAR */
  .stats-bar {
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    display: flex;
    padding: 0 48px;
    overflow-x: auto;
  }

  .stat {
    padding: 24px 48px 24px 0;
    flex-shrink: 0;
  }

  .stat:not(:last-child) {
    border-right: 1px solid var(--border);
    margin-right: 48px;
  }

  .stat-num {
    font-family: 'Syne', sans-serif;
    font-size: 32px;
    font-weight: 800;
    color: var(--accent);
    letter-spacing: -1px;
  }

  .stat-label {
    font-size: 11px;
    color: var(--muted);
    letter-spacing: 1px;
    text-transform: uppercase;
    margin-top: 4px;
  }

  /* WHATSAPP DEMO */
  .demo-section {
    padding: 100px 48px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .demo-label {
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 20px;
  }

  .demo-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(32px, 4vw, 48px);
    font-weight: 800;
    letter-spacing: -1.5px;
    line-height: 1.05;
    margin-bottom: 20px;
  }

  .demo-desc {
    color: var(--muted);
    font-size: 14px;
    line-height: 1.8;
    font-weight: 300;
  }

  /* Phone mockup */
  .phone {
    background: #1a1a1a;
    border: 2px solid var(--border);
    border-radius: 36px;
    padding: 20px 16px;
    max-width: 300px;
    margin: 0 auto;
    position: relative;
    box-shadow: 0 40px 80px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,255,255,0.05);
  }

  .phone-notch {
    width: 80px; height: 6px;
    background: #000;
    border-radius: 4px;
    margin: 0 auto 16px;
  }

  .phone-header {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 8px 12px;
    background: #075E54;
    border-radius: 12px 12px 0 0;
    margin-bottom: 2px;
  }

  .phone-avatar {
    width: 36px; height: 36px;
    background: var(--accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
  }

  .phone-name {
    font-family: 'Syne', sans-serif;
    font-weight: 700;
    font-size: 14px;
  }

  .phone-status {
    font-size: 11px;
    color: rgba(255,255,255,0.6);
  }

  .phone-body {
    background: #0b141a;
    border-radius: 0 0 12px 12px;
    padding: 12px;
    min-height: 360px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .msg {
    max-width: 85%;
    padding: 8px 12px;
    border-radius: 8px;
    font-size: 12px;
    line-height: 1.5;
    animation: msgIn 0.3s ease both;
  }

  .msg-bot {
    background: #202c33;
    align-self: flex-start;
    border-radius: 0 8px 8px 8px;
  }

  .msg-user {
    background: #005c4b;
    align-self: flex-end;
    border-radius: 8px 0 8px 8px;
  }

  .msg-time {
    font-size: 10px;
    color: rgba(255,255,255,0.4);
    text-align: right;
    margin-top: 3px;
  }

  .msg-alert {
    background: rgba(0,232,122,0.1);
    border: 1px solid rgba(0,232,122,0.3);
    border-left: 3px solid var(--accent);
    color: var(--text);
  }

  .msg-alert strong { color: var(--accent); display: block; margin-bottom: 4px; }

  @keyframes msgIn {
    from { opacity: 0; transform: translateY(8px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* FEATURES */
  .features-section {
    padding: 100px 48px;
    background: var(--surface);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .section-header {
    text-align: center;
    margin-bottom: 64px;
  }

  .section-label {
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 16px;
  }

  .section-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(32px, 4vw, 52px);
    font-weight: 800;
    letter-spacing: -2px;
    line-height: 1.05;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .feature-card {
    background: var(--card);
    padding: 36px 32px;
    border: 1px solid var(--border);
    transition: all 0.3s;
    position: relative;
    overflow: hidden;
  }

  .feature-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.3s;
  }

  .feature-card:hover { background: #161616; transform: translateY(-2px); }
  .feature-card:hover::before { transform: scaleX(1); }

  .feature-icon {
    font-size: 28px;
    margin-bottom: 20px;
  }

  .feature-title {
    font-family: 'Syne', sans-serif;
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 10px;
  }

  .feature-desc {
    color: var(--muted);
    font-size: 13px;
    line-height: 1.7;
    font-weight: 300;
  }

  /* PRICING */
  .pricing-section {
    padding: 100px 48px;
    max-width: 1000px;
    margin: 0 auto;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
    margin-top: 64px;
  }

  .pricing-card {
    background: var(--card);
    border: 1px solid var(--border);
    padding: 40px;
    position: relative;
    transition: all 0.3s;
  }

  .pricing-card:hover { border-color: rgba(0,232,122,0.3); }

  .pricing-card.featured {
    border-color: var(--accent);
    background: rgba(0,232,122,0.03);
  }

  .pricing-badge {
    position: absolute;
    top: -12px; left: 40px;
    background: var(--accent);
    color: #000;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 1px;
    text-transform: uppercase;
    padding: 4px 12px;
  }

  .pricing-name {
    font-family: 'Syne', sans-serif;
    font-size: 22px;
    font-weight: 800;
    margin-bottom: 8px;
  }

  .pricing-price {
    font-family: 'Syne', sans-serif;
    font-size: 52px;
    font-weight: 800;
    letter-spacing: -2px;
    color: var(--accent);
    line-height: 1;
    margin: 20px 0 4px;
  }

  .pricing-price span {
    font-size: 18px;
    font-weight: 400;
    color: var(--muted);
    letter-spacing: 0;
  }

  .pricing-desc {
    color: var(--muted);
    font-size: 13px;
    margin-bottom: 32px;
    font-weight: 300;
  }

  .pricing-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 36px;
  }

  .pricing-features li {
    font-size: 13px;
    color: var(--muted);
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 300;
  }

  .pricing-features li::before {
    content: '✓';
    color: var(--accent);
    font-weight: 700;
    flex-shrink: 0;
  }

  .btn-outline {
    display: block;
    text-align: center;
    border: 1px solid var(--border);
    color: var(--text);
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    padding: 14px;
    text-decoration: none;
    transition: all 0.2s;
    letter-spacing: 0.5px;
  }

  .btn-outline:hover { border-color: var(--accent); color: var(--accent); }

  .btn-filled {
    display: block;
    text-align: center;
    background: var(--accent);
    color: #000;
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    padding: 14px;
    text-decoration: none;
    transition: all 0.2s;
    letter-spacing: 0.5px;
    font-weight: 500;
  }

  .btn-filled:hover { background: #00ff88; }

  /* CTA SECTION */
  .cta-section {
    padding: 100px 48px;
    text-align: center;
    background: var(--surface);
    border-top: 1px solid var(--border);
    position: relative;
    overflow: hidden;
  }

  .cta-glow {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 600px; height: 300px;
    background: radial-gradient(ellipse, rgba(0,232,122,0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-section h2 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(36px, 5vw, 64px);
    font-weight: 800;
    letter-spacing: -2px;
    line-height: 1.05;
    margin-bottom: 20px;
    position: relative;
  }

  .cta-section p {
    color: var(--muted);
    font-size: 15px;
    margin-bottom: 40px;
    font-weight: 300;
    position: relative;
  }

  /* WAITLIST FORM */
  .waitlist-form {
    display: flex;
    gap: 0;
    max-width: 440px;
    margin: 0 auto;
    position: relative;
  }

  .waitlist-form input {
    flex: 1;
    background: var(--card);
    border: 1px solid var(--border);
    border-right: none;
    color: var(--text);
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    padding: 14px 20px;
    outline: none;
    transition: border-color 0.2s;
  }

  .waitlist-form input:focus { border-color: var(--accent); }
  .waitlist-form input::placeholder { color: var(--muted); }

  .waitlist-form button {
    background: var(--accent);
    color: #000;
    border: none;
    font-family: 'DM Mono', monospace;
    font-size: 13px;
    font-weight: 500;
    padding: 14px 24px;
    cursor: pointer;
    letter-spacing: 0.5px;
    transition: background 0.2s;
    white-space: nowrap;
  }

  .waitlist-form button:hover { background: #00ff88; }

  /* FOOTER */
  footer {
    padding: 32px 48px;
    border-top: 1px solid var(--border);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 16px;
  }

  .footer-logo span { color: var(--accent); }

  .footer-note {
    font-size: 12px;
    color: var(--muted);
  }

  /* TICKER */
  .ticker {
    background: var(--accent);
    color: #000;
    padding: 10px 0;
    overflow: hidden;
    white-space: nowrap;
  }

  .ticker-inner {
    display: inline-block;
    animation: ticker 20s linear infinite;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1px;
  }

  .ticker-inner span { margin: 0 40px; }

  @keyframes ticker {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* Success state */
  .success-msg {
    display: none;
    color: var(--accent);
    font-size: 13px;
    margin-top: 12px;
    text-align: center;
  }

  @media (max-width: 768px) {
    nav { padding: 16px 24px; }
    .hero { padding: 100px 24px 60px; }
    .demo-section { grid-template-columns: 1fr; padding: 60px 24px; gap: 48px; }
    .features-section, .pricing-section, .cta-section { padding: 60px 24px; }
    .stats-bar { padding: 0 24px; }
    .pricing-grid { grid-template-columns: 1fr; }
    footer { flex-direction: column; gap: 12px; text-align: center; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="nav-logo">Claw<span>Commerce</span></div>
  <a href="#waitlist" class="nav-cta">Join Waitlist →</a>
</nav>

<!-- TICKER -->
<div class="ticker" style="margin-top: 65px;">
  <div class="ticker-inner">
    <span>🦞 YOUR SHOPIFY AI EMPLOYEE</span>
    <span>📲 WORKS ON WHATSAPP</span>
    <span>⚡ 24/7 STORE MONITORING</span>
    <span>💰 INCREASE YOUR REVENUE</span>
    <span>🦞 YOUR SHOPIFY AI EMPLOYEE</span>
    <span>📲 WORKS ON WHATSAPP</span>
    <span>⚡ 24/7 STORE MONITORING</span>
    <span>💰 INCREASE YOUR REVENUE</span>
    <span>🦞 YOUR SHOPIFY AI EMPLOYEE</span>
    <span>📲 WORKS ON WHATSAPP</span>
    <span>⚡ 24/7 STORE MONITORING</span>
    <span>💰 INCREASE YOUR REVENUE</span>
  </div>
</div>

<!-- HERO -->
<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-tag">Powered by OpenClaw + Shopify</div>
  <h1>Your Shopify store<br>needs an <em>AI employee</em></h1>
  <p class="hero-sub">ClawCommerce watches your store 24/7 and pings you on WhatsApp when it's time to act — price drops, stock alerts, ad performance, and more.</p>
  <div class="hero-actions">
    <a href="#waitlist" class="btn-primary">Get Early Access →</a>
    <a href="#features" class="btn-ghost">See what it does</a>
  </div>
</section>

<!-- STATS -->
<div class="stats-bar">
  <div class="stat">
    <div class="stat-num">24/7</div>
    <div class="stat-label">Store Monitoring</div>
  </div>
  <div class="stat">
    <div class="stat-num">$0</div>
    <div class="stat-label">Extra Tools Needed</div>
  </div>
  <div class="stat">
    <div class="stat-num">5min</div>
    <div class="stat-label">Setup Time</div>
  </div>
  <div class="stat">
    <div class="stat-num">WhatsApp</div>
    <div class="stat-label">Your Interface</div>
  </div>
</div>

<!-- DEMO -->
<section class="demo-section">
  <div>
    <div class="demo-label">// How it works</div>
    <h2 class="demo-title">Your store talks to you. Finally.</h2>
    <p class="demo-desc">Connect your Shopify store once. ClawCommerce runs in the background, watching every metric that matters. When something needs your attention, it messages you on WhatsApp — no dashboards, no logins, no noise.</p>
    <br>
    <p class="demo-desc">Reply back in plain English to take action. It's like having a store manager who never sleeps.</p>
  </div>

  <div class="phone">
    <div class="phone-notch"></div>
    <div class="phone-header">
      <div class="phone-avatar">🦞</div>
      <div>
        <div class="phone-name">ClawCommerce</div>
        <div class="phone-status">online</div>
      </div>
    </div>
    <div class="phone-body">
      <div class="msg msg-bot msg-alert" style="animation-delay:0.2s">
        <strong>⚠️ Competitor Price Drop</strong>
        "Nike Air Max 90" on StoreX dropped to $89. You're at $109. Want me to match it?
        <div class="msg-time">09:14</div>
      </div>
      <div class="msg msg-user" style="animation-delay:0.6s">
        Drop to $94, not lower
        <div class="msg-time">09:16</div>
      </div>
      <div class="msg msg-bot" style="animation-delay:1s">
        ✅ Done. Price updated to $94. I'll monitor conversion for the next 2hrs.
        <div class="msg-time">09:16</div>
      </div>
      <div class="msg msg-bot msg-alert" style="animation-delay:1.4s">
        <strong>📦 Low Stock Alert</strong>
        "Black Hoodie (M)" has 3 units left. Avg daily sales: 4. Reorder now?
        <div class="msg-time">11:32</div>
      </div>
      <div class="msg msg-user" style="animation-delay:1.8s">
        Yes, order 50 units from supplier A
        <div class="msg-time">11:33</div>
      </div>
      <div class="msg msg-bot" style="animation-delay:2.2s">
        ✅ Purchase order drafted & sent. ETA: 5 days.
        <div class="msg-time">11:33</div>
      </div>
    </div>
  </div>
</section>

<!-- FEATURES -->
<section class="features-section" id="features">
  <div class="section-header">
    <div class="section-label">// Features</div>
    <h2 class="section-title">Everything your store needs.<br>In your pocket.</h2>
  </div>
  <div class="features-grid">
    <div class="feature-card">
      <div class="feature-icon">📊</div>
      <div class="feature-title">Daily Revenue Digest</div>
      <div class="feature-desc">Wake up to a WhatsApp summary of yesterday's sales, top products, and what to focus on today.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon">🔍</div>
      <div class="feature-title">Competitor Price Monitor</div>
      <div class="feature-desc">Track your competitors' prices automatically. Get alerted the moment they undercut you — and act in one reply.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon">📦</div>
      <div class="feature-title">Smart Inventory Alerts</div>
      <div class="feature-desc">Never run out of stock again. ClawCommerce predicts when you'll sell out and nudges you to reorder before it's too late.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon">⭐</div>
      <div class="feature-title">Review Responder</div>
      <div class="feature-desc">Get new reviews sent to WhatsApp with a suggested reply. Approve or edit in seconds — no copy-pasting needed.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon">🛒</div>
      <div class="feature-title">Abandoned Cart Recovery</div>
      <div class="feature-desc">See abandoned carts in real time with suggested recovery actions. One message to trigger a discount or follow-up email.</div>
    </div>
    <div class="feature-card">
      <div class="feature-icon">📈</div>
      <div class="feature-title">Ad Performance Alerts</div>
      <div class="feature-desc">Get notified when your ROAS drops or a campaign is crushing it. Scale winners and kill losers without opening Meta Ads Manager.</div>
    </div>
  </div>
</section>

<!-- PRICING -->
<section class="pricing-section" id="pricing">
  <div class="section-header">
    <div class="section-label">// Pricing</div>
    <h2 class="section-title">Less than one sale<br>pays for itself.</h2>
  </div>
  <div class="pricing-grid">
    <div class="pricing-card">
      <div class="pricing-name">Starter</div>
      <div class="pricing-price">$49<span>/mo</span></div>
      <div class="pricing-desc">For solo Shopify sellers</div>
      <ul class="pricing-features">
        <li>1 Shopify store</li>
        <li>WhatsApp + Telegram</li>
        <li>Daily revenue digest</li>
        <li>Inventory alerts</li>
        <li>Competitor monitoring (3 products)</li>
        <li>Email support</li>
      </ul>
      <a href="#waitlist" class="btn-outline">Get Started →</a>
    </div>
    <div class="pricing-card featured">
      <div class="pricing-badge">Most Popular</div>
      <div class="pricing-name">Growth</div>
      <div class="pricing-price">$99<span>/mo</span></div>
      <div class="pricing-desc">For serious store owners</div>
      <ul class="pricing-features">
        <li>Up to 3 Shopify stores</li>
        <li>All messaging platforms</li>
        <li>Everything in Starter</li>
        <li>Abandoned cart recovery</li>
        <li>Ad performance tracking</li>
        <li>Unlimited competitor monitoring</li>
        <li>Review responder</li>
        <li>Priority support</li>
      </ul>
      <a href="#waitlist" class="btn-filled">Get Started →</a>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section" id="waitlist">
  <div class="cta-glow"></div>
  <h2>Join the waitlist.<br>Launch is coming soon.</h2>
  <p>Be first to get access — early members get 30% off forever.</p>
  <form class="waitlist-form" onsubmit="handleSubmit(event)">
    <input type="email" placeholder="your@email.com" required id="emailInput">
    <button type="submit">Join Waitlist →</button>
  </form>
  <p class="success-msg" id="successMsg">🎉 You're on the list! We'll be in touch soon.</p>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-logo">Claw<span>Commerce</span></div>
  <div class="footer-note">Built on OpenClaw · Your data stays yours · © 2026</div>
</footer>

<script>
  function handleSubmit(e) {
    e.preventDefault();
    const email = document.getElementById('emailInput').value;
    document.querySelector('.waitlist-form').style.display = 'none';
    document.getElementById('successMsg').style.display = 'block';
    console.log('Waitlist signup:', email);
  }

  // Animate stats on scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(el => {
      if (el.isIntersecting) el.target.style.opacity = '1';
    });
  });

  document.querySelectorAll('.feature-card, .pricing-card').forEach(el => {
    el.style.opacity = '0';
    el.style.transition = 'opacity 0.5s ease, transform 0.3s ease, background 0.3s, border-color 0.3s';
    observer.observe(el);
  });
</script>
</body>
</html>
